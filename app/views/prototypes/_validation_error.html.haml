- if @prototype.errors.any?
  #error_explanation
    %ul.alert-error.alert
      = link_to "×", "#", "data-dismiss": "alert", class: "close"
      - @prototype.errors.full_messages.each do |msg|
        %li= msg
